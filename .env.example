# 环境变量模板。复制为 .env 并填真实值。.env 已被 .gitignore 忽略，永不提交。

# 大脑：OpenAI 兼容端点
LLM_BASE_URL=https://kspmas.ksyun.com/v1
LLM_MODEL=ep-20260613040107-bimti
LLM_API_KEY=__在_.env_里填_勿入库__

# 说明：
# - 该端点已验证可用（2026-07-03，回 pong，真实模型 mco-6）
# - key 一律走环境变量，绝不写入任何提交文件
# - OpenHands/LiteLLM 接入时 model 用 openai/ 前缀：openai/${LLM_MODEL}

# 阶段 2 数据源：NCBI 礼貌规则要求 contact email（pubmed / clinvar / dbsnp 等
# E-utilities 源）。不填会被 NCBI 拒（"NCBI etiquette requires a contact email"）。
# 有 API key 可提配额（可选）。operon 读的就是这两个同名变量。
NCBI_EMAIL=__你的真实邮箱__
# NCBI_API_KEY=__可选__
